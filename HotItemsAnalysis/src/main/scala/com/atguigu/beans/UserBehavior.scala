package com.atguigu.beans

case class UserBehavior(useId: Long,
                        itemId: Long,
                        categoryId: Int,
                        behavior: String,
                        timestamp: Long)
